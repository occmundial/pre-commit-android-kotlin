- id: unit_tests
  name: unittests
  description: Runs unit tests
  entry: cd OCCMundial && bash ./gradlew test
  pass_filenames: false
  language: system

- id: build
  name: build
  description: Building App
  entry: cd OCCMundial && bash ./gradlew assembleDebug
  pass_filenames: false
  language: system
